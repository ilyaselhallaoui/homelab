apiVersion: v1
kind: PersistentVolume
metadata:
  name: firefly-pv
spec:
  capacity:
    storage: 5Gi
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/data/firefly
