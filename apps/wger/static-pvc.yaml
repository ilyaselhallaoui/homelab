apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-wger-pvc
  namespace: wger
spec:
  storageClassName: local-path
  volumeMounts: static-wger-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
    storage: 500Mi
