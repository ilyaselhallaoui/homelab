apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-wger-pvc
  namespace: wger
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
