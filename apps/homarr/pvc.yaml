apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-pvc
  namespace: homarr
spec:
  storageClassName: ""
  volumeName: homarr-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

